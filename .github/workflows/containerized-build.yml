name: Containerised Firmware Build

on:
  workflow_call:
    inputs:
      firmware:
        required: true
        type: string
      target:
        required: true
        type: string
      vendor:
        default: "Cypherock"
        type: string
      coin_support_variant:
        default: 'MULTI_COIN'
        type: string

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: cypherock/x1-firmware-builder:v1.0.0

    steps:
    - name: Set VERSION_TAG
      id: set_version_tag
      run: |
        if [[ "${{ github.ref_type }}" == "tag" ]]; then
          echo "VERSION_TAG=$(echo "${{ github.ref }}" | cut -f 3- -d '/')" >> $GITHUB_OUTPUT
        elif [[ "${{ github.ref_type }}" == "branch" ]]; then
          echo "VERSION_TAG=$(echo "${{ github.ref }}" | cut -f 3- -d '/')" >> $GITHUB_OUTPUT
        else
          echo "VERSION_TAG=main" >> $GITHUB_OUTPUT
        fi

    - name: Checkout Firmware Repository
      uses: actions/checkout@v4
      with:
        repository: Cypherock/x1_wallet_firmware
        ref: ${{ steps.set_version_tag.outputs.VERSION_TAG }}
        submodules: recursive
        path: x1_wallet_firmware

    - name: Build Firmware (${{ inputs.firmware }} - ${{ inputs.target }} - ${{ inputs.vendor }} - ${{ inputs.coin_support_variant }})
      shell: bash
      run: |
        cd x1_wallet_firmware

        BTC_ONLY_FLAG="OFF"
        if [[ "${{ inputs.coin_support_variant }}" == "BTC_ONLY" ]]; then
          BTC_ONLY_FLAG="ON"
        fi

        BUILD_DIR="build_${{ inputs.vendor }}_${{ inputs.coin_support_variant }}"
        rm -rf "${BUILD_DIR}"
        mkdir -p "${BUILD_DIR}"


        cmake -B "${BUILD_DIR}" \
              -DDEV_SWITCH=OFF \
              -DUNIT_TESTS_SWITCH:BOOL=OFF \
              -DBTC_ONLY:BOOL="${BTC_ONLY_FLAG}" \
              -DSIGN_BINARY=OFF \
              -DCMAKE_BUILD_TYPE:STRING="${{ inputs.target }}" \
              -DFIRMWARE_TYPE="${{ inputs.firmware }}" \
              -DCMAKE_EXPORT_COMPILE_COMMANDS:BOOL=OFF \
              -DCMAKE_BUILD_PLATFORM:STRING="Device" \
              -DVENDOR="${{ inputs.vendor }}" \
              -G "Ninja" .

        ninja -C "${BUILD_DIR}"
        
        cd .. 

        mkdir -p build

        cp x1_wallet_firmware/"${BUILD_DIR}"/*.elf ./build/ 2>/dev/null || true
        cp x1_wallet_firmware/"${BUILD_DIR}"/*.bin ./build/ 2>/dev/null || true
        cp x1_wallet_firmware/"${BUILD_DIR}"/*.hex ./build/ 2>/dev/null || true

        EXPECTED_NAME="${{ inputs.vendor }}-${{ inputs.firmware }}"
        if [[ "${{ inputs.coin_support_variant }}" == "BTC_ONLY" ]]; then
          EXPECTED_NAME="${EXPECTED_NAME}-BTC"
        fi
        for f in ./build/*; do
          ext="${f##*.}"
          target="./build/${EXPECTED_NAME}.${ext}"
          if [ "$f" != "$target" ]; then
            mv "$f" "$target"
          fi
        done

        echo "--- Copied to ./build ---"
        ls -la ./build/
        echo "--- end listing ---"

    - name: Archive Build Artifacts (${{ inputs.vendor }}-${{ inputs.coin_support_variant }})
      uses: actions/upload-artifact@v4
      with:
        name: ${{ inputs.firmware }}-${{ inputs.target }}-${{ inputs.vendor }}-${{ inputs.coin_support_variant }}-outputs
        path: build
